# 浩德堂網站設定說明

網站是純靜態檔案（HTML + CSS + JS），所有日常設定都集中在 **`js/config.js`** 這一個檔案，
改完存檔、推送到 GitHub，Vercel 就會自動更新，不需要重新部署。

---

## 一、設定網站正式網址（最重要）

開啟 `js/config.js`，最上方：

```js
SITE: {
  name: "浩德堂",
  url: "https://hautertang-website.vercel.app/",   // ← 改成你的正式網址
  desc: "浩德堂 ｜ 讓敬心有處安住，讓善念有路可回。",
  qrCode: "assets/images/site-qr.png"
}
```

這個網址會影響三件事：

| 項目 | 說明 |
|---|---|
| 分享按鈕 | Facebook / LINE / Instagram 分享出去的連結 |
| QR Code | 網站上的 QR Code 會**自動重新產生**，不用換圖 |
| OG 分享預覽 | 分享到社群時顯示的標題與縮圖 |

> 換網域時，除了這裡，也要同步改 `index.html` 第 12～19 行的
> `og:image` 與 `og:url`（兩處網址要一致）。

---

## 二、綠界支付設定

已串接**綠界全方位金流（正式環境）**，訪客送出登記後會自動導向綠界付款頁，
可選擇信用卡、Apple Pay、綠界Pay，並自動帶入本次的供養金額。

相關的金鑰設定、部署方式、搬到自己主機的步驟，
請看另一份文件：**《浩德堂_綠界金流設定說明.md》**。

> ⚠️ 綠界的 `HashKey` / `HashIV` 只存放在後端環境變數，**不會**出現在 `js/config.js`。
> 前端設定檔裡只需要維持這樣即可，不要填入任何金鑰：

```js
ECPAY: {
  enabled: true,
  apiUrl: "/api/create-order",   // 後端 API，不需要改
  label: "綠界支付",
  note: "送出登記後會導向綠界支付頁面。"
}
```

---

## 三、供養項目與金額調整

項目定義統一放在 **`lib/items.js`**（前端畫面與後端驗證共用同一份），
一項就是一張勾選卡：

```js
{
  id: "taisu-tile",              // 代號，不可重複
  group: "太素觀供養銅瓦",        // 分組標題
  name: "供養銅瓦",               // 顯示名稱
  type: "unit",                  // fixed=固定金額 / free=隨喜 / unit=單位計價
  unitAmount: 500,               // 每單位 500 元
  unitPerSet: 4,                 // 1 片 = 4 單位
  desc: "每單位 500 元，1 片 = 4 單位 = 2,000 元"
}
```

**三種計價方式：**

| type | 用途 | 必填欄位 |
|---|---|---|
| `fixed` | 固定金額（供花、供果、供燈） | `amount` |
| `free` | 隨喜（禮敬上香） | 無 |
| `unit` | 單位計價（供養銅瓦） | `unitAmount`、`unitPerSet` |

**新增項目**：複製一組 `{...}` 貼上，改掉 `id` 與名稱金額即可。
**暫停某項目**：在該組前後加 `/* ... */` 註解掉，或直接刪除。

> `id` 不能重複，也不要用中文，否則勾選與金額計算會失效。
>
> 改完這裡，前端選單與後端的金額驗證會**同時生效**，
> 不需要再改 `js/config.js`（它只是把這份定義讀進來用）。

---

## 三之一、付款方式（綠界）

訪客在植福田頁勾選完供養項目後，會在**站內**先選好付款方式，
送出後綠界就直接開「對應的那一個」付款頁，不需要再選一次。

### 為什麼卡號不能在我們網站輸入？

信用卡卡號若經過我們的伺服器，就違反 PCI DSS 規範與綠界的特約商店合約，
一旦發生盜刷糾紛，責任會落在浩德堂身上。
所以「輸入卡號」這一步一定得在綠界頁面完成，這是所有金流服務的共同做法。

網站上也會明確告訴訪客：
「付款資料由綠界科技處理，本站不會取得或保存您的卡號。」

### 如何新增付款方式

付款方式定義在 `lib/items.js` 的 `PAYMENT_METHODS`：

```js
{
  id: "ATM",
  name: "ATM 虛擬帳號",
  desc: "取得專屬帳號後轉帳或臨櫃繳費",
  enabled: false,        // ← 改成 true 就會出現在網站上
  ecpayCode: "ATM"       // 綠界的 ChoosePayment 代碼，不要改
}
```

**必須先到綠界後台申請開通該付款方式**，再把 `enabled` 改成 `true`。
若網站開了、綠界沒開通，訪客點下去會看到綠界的錯誤訊息。

目前浩德堂已開通：信用卡、Apple Pay。
程式裡已預留但尚未開通：ATM 虛擬帳號、超商代碼、超商條碼。

> 補充：選「信用卡」時，綠界頁面上會同時出現
> 「信用卡 / Apple Pay / 綠界Pay」三個頁籤（綠界把這三種歸在同類），
> 但預設停在信用卡，訪客直接填卡號即可。
> 若選「Apple Pay」則直接進 Apple Pay 專屬頁面。

---

## 三之二、QR Code 放在哪裡

網站目前有三個地方會出現 QR Code，各有不同用途：

| 位置 | 尺寸 | 用途 |
|---|---|---|
| **聯絡承接頁**（主要據點） | 220px | 信眾想分享給親友時，會來這裡找 |
| 每一頁底部的分享區 | 92px | 隨手可掃，瀏覽到任何一頁都能用 |
| 點 QR 後放大顯示 | 300px | 手機對著別人的螢幕掃描 |

另外在聯絡頁提供 **「下載 QR Code 圖檔」** 按鈕，
下載的是一張 1000 × 1400 的海報（含浩德堂標題、網址），
適合印在 DM、名片、LINE 社群封面或張貼在佛堂。

QR Code 是**即時產生**的，改了 `SITE.url` 就會自動更新，
只有「下載的圖檔」需要重新產生（網址異動時告訴我即可）。

---

## 四、分享功能開關

```js
SHARE: {
  facebook: true,
  line: true,
  instagram: true,
  copyLink: true,
  qrCode: true
}
```

把 `true` 改成 `false` 即可隱藏該按鈕。
「更多分享」按鈕只會在手機瀏覽器出現（使用手機系統的分享選單），電腦自動隱藏。

### Instagram 為什麼是「複製連結」？

Instagram 沒有開放網頁版的分享 API（只有 App 能發文），
所以點下去會先幫訪客複製網址，再開啟 Instagram，由訪客貼上分享。

---

## 五、更新流程

```bash
git add .
git commit -m "更新說明"
git push
```

推送後 Vercel 約 1～2 分鐘自動部署完成。

若改了 CSS 或 JS 後覺得畫面沒變，通常是瀏覽器快取，
請用 `Ctrl + F5`（Windows）或 `Cmd + Shift + R`（Mac）強制重新整理。
