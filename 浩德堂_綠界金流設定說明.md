# 浩德堂 綠界金流設定與部署說明

## 一、整體架構（為什麼需要後端）

綠界的 `HashKey` / `HashIV` 是簽章用的密鑰。
**只要放在前端（HTML / JS），任何人按 F12 就能看到，進而偽造訂單、把 8,500 元改成 1 元。**
所以必須由後端負責「算金額 → 簽章 → 產生訂單」，前端只負責把訪客送去綠界。

```
訪客勾選項目
    ↓
前端送出「勾選了哪些項目」
    ↓
後端 /api/create-order
   ├─ 依項目定義重新計算金額（不採信前端傳來的數字）
   ├─ 寫入 Google 試算表（狀態：待付款）
   └─ 產生 CheckMacValue 簽章，回傳綠界表單參數
    ↓
前端自動 POST 到綠界付款頁
    ↓
訪客在綠界付款（信用卡 / Apple Pay / 綠界Pay）
    ↓
綠界伺服器 POST 到 /api/ecpay-notify
   ├─ 驗證 CheckMacValue（確認真的是綠界送來的）
   └─ 更新試算表該筆訂單為「已付款」
    ↓
綠界把訪客導回 #payresult 感謝頁
```

**搬遷設計**：核心邏輯都在 `lib/ecpay.js` 與 `lib/items.js`，
Vercel 與自己的主機共用同一份程式碼，搬到自己的 server 不需要改寫。

---

## 二、檔案說明

| 檔案 | 用途 |
|---|---|
| `api/create-order.js` | 建立訂單、計算金額、寫入試算表 |
| `api/ecpay-notify.js` | 接收綠界付款通知、驗證簽章、更新狀態 |
| `lib/ecpay.js` | CheckMacValue 產生與驗證（核心） |
| `lib/items.js` | 供養項目與金額定義（前後端共用同一份） |
| `api/_env.js` | 讀取環境變數 |
| `server/server.js` | 自己主機用的 Node 伺服器（零套件依賴） |

---

## 三、在 Vercel 上啟用（已完成 ✅）

金鑰已經透過 Vercel API 設定完成，並已重新部署。

**正式網址：https://hautertang-website.vercel.app/**

設定值如下（`HashKey` / `HashIV` 在 Vercel 上是加密存放，連專案後台也看不到明文）：

| 名稱 | 值 | 類型 |
|---|---|---|
| `ECPAY_MERCHANT_ID` | `3508165` | plain |
| `ECPAY_HASH_KEY` | `CDYw7AS4pksSFIX8` | encrypted（加密） |
| `ECPAY_HASH_IV` | `UxblpvUIDZGJVmFk` | encrypted（加密） |
| `ECPAY_MODE` | `production` | plain |
| `SITE_URL` | `https://hautertang-website.vercel.app` | plain |
| `GAS_URL` | （尚未設定） | — |

同時也關閉了專案的 **Vercel SSO 保護**
（原本設定為「除了自訂網域以外的所有部署都要登入」，
這就是之前用手機開網址會跳出 Vercel 登入頁的原因）。
現在任何人掃 QR Code 都能直接進站。

### 快速確認設定是否就位

瀏覽器直接開這個網址：

```
https://hautertang-website.vercel.app/api/health
```

會看到 JSON 格式的健檢結果。金鑰**只顯示前後各 2 碼**，中間遮罩，
可以確認「有沒有設定」與「設得對不對」，但不會外洩：

```json
{
  "ok": true,
  "商店代號": "35●●●65",
  "HashKey": "CD●●●●●●●●●●●●X8",
  "HashIV": "Ux●●●●●●●●●●●●Fk",
  "環境": "正式環境 (production)",
  "綠界端點": "https://payment.ecpay.com.tw/Cashier/AioCheckOut/V5",
  "網站網址": "https://hautertang-website.vercel.app",
  "試算表": "未設定（訂單不會寫入試算表）",
  "簽章演算法": "正常",
  "缺少的環境變數": []
}
```

只要 `"ok": true` 就是設定正確。

> ⚠️ 這些值**不要**寫進任何會進 Git 的檔案。
> `.gitignore` 已經排除 `.env`，本機測試用 `.env` 即可。

---

## 四、注意：目前開通的付款方式

實測連到你的正式商店（商店名稱顯示為**浩德堂Hauter**）時，綠界只提供：

- ✅ 信用卡（一次付清 / 分期）
- ✅ Apple Pay
- ✅ 綠界Pay

**沒有** ATM 虛擬帳號、超商代碼、超商條碼、LINE Pay。

如果希望信眾能用 ATM 轉帳或超商繳費，需要到綠界後台
「全方位金流」→「付款方式」申請開通（部分需要審核與額外合約）。
開通後網站不用改，`ChoosePayment: ALL` 會自動帶出所有已開通的方式。

---

## 五、搬到自己的主機

`server/server.js` 是純 Node.js 寫的，**不需要安裝任何套件**：

```bash
# 1. 把整個資料夾複製到主機
# 2. 建立 .env（內容同第三節的表格）
cp .env.example .env
vi .env

# 3. 啟動
node server/server.js
# 或指定 port
PORT=8080 node server/server.js
```

建議用 PM2 或 systemd 讓它在背景常駐：

```bash
npm i -g pm2
pm2 start server/server.js --name haodetang
pm2 save && pm2 startup
```

### 兩個一定要做的事

1. **要有 HTTPS**。綠界要求 `ReturnURL` 必須是 `https://`，
   建議用 Nginx + Let's Encrypt 反向代理到 Node 的 port。
2. **對外防火牆只開 80 / 443**，Node 的 port（如 3000）不要直接對外。

Nginx 參考設定：

```nginx
server {
    listen 443 ssl;
    server_name 你的網域;

    ssl_certificate     /etc/letsencrypt/live/你的網域/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/你的網域/privkey.pem;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

---

## 六、Google 試算表：更新 GAS 腳本

為了記錄「訂單編號」與「是否已付款」，GAS 腳本需要換成新版。

### 步驟

1. 開啟你的 Google 試算表 → 擴充功能 → Apps Script
2. 刪除原本程式碼，貼上 `GoogleAppsScript_新版.js` 的全部內容
3. 「部署」→「**管理部署**」→ 編輯現有部署 → 版本選「建立新版本」
   （這樣**網址不會變**，不用回去改環境變數）
4. 在「明細紀錄」工作表第一列補上標題（若原本只有 A~K）：

   | A | B | C | D | E | F | G | H | I | J | K | L | M | N |
   |---|---|---|---|---|---|---|---|---|---|---|---|---|---|
   | 日期 | 月份 | 姓名／稱呼 | 聯絡方式 | 方向 | 項目 | 單位數 | 單位金額 | 本筆金額 | 備註 | 是否已匯款 | **訂單編號** | **付款時間** | **付款方式** |

   L、M、N 是新加的，A~K 維持原樣，既有的 SUMIFS 公式不受影響。

### 資料寫入時機

| 時機 | 寫入內容 | 「是否已匯款」欄 |
|---|---|---|
| 訪客送出登記 | 完整明細（含項目、數量、金額、備註） | `待付款` |
| 綠界通知付款成功 | 以訂單編號比對，更新付款時間與方式 | `已付款` |

即使綠界通知失敗，登記資料也已經在試算表裡，不會遺失。

---

## 七、上線前檢查清單

- [ ] Vercel 環境變數已設定並 Redeploy
- [ ] 用 `SITE_URL` 指向正確的正式網址
- [ ] 綠界後台的「ReturnURL 設定」允許你的網域（若後台有此項目）
- [ ] 實測一筆小額（例如 10 元）走完整個流程
- [ ] 確認試算表有寫入，且付款後狀態變成「已付款」
- [ ] 確認付款完成會導回 `#payresult` 感謝頁

---

## 八、安全建議

1. **建議在測試完成後重新產生 HashKey / HashIV。**
   這組金鑰曾經以明文方式在對話中傳遞，雖然不會外流，
   但重新產生成本很低（綠界後台可重新產生），是比較好的習慣。
   重新產生後，記得同步更新 Vercel 的環境變數。

2. **不要**把 `.env`、金鑰貼進任何會進 Git 的檔案。

3. 定期到綠界後台查看「交易明細」，與試算表核對。
